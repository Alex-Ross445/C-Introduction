#include <stdio.h>
#include <limits.h>

int main() {
	unsigned int x = UNIT_MAX - 1;
	signed int y = INT_MAX - 1;

	printf("x is an unsigned int, occupying %i bytes.\n\n", sizeof(x));

	printf("The\n");
}